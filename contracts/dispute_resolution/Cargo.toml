[package]
name = "dispute_resolution"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "22.0", default-features = false }

[dev_dependencies]
soroban-sdk = { version = "22.0", features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils"]