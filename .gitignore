/target
node_modules/
**/node_modules/
**/test_snapshots
**/test_snapshots/
*.snap
*.snapshot
*.local
.DS_Store
.env
.env.*
.cargo_home/

# proptest regression files are captured as CI artifacts (see .github/workflows/fuzz.yml).
# They are intentionally excluded from version control to avoid noisy diffs, but the
# artifact cache in CI ensures discovered counterexamples are never lost.
**/proptest-regressions/
