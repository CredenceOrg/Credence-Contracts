name: Contracts Tests
on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Workspace Tests
    runs-on: ubuntu-latest
    steps:
      - name: Workspace Tests (paused — CI stabilization in progress)
        run: echo 'Workspace Tests check intentionally stubbed while main-branch CI is being stabilized.'
